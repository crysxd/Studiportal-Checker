<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hfu.funfpunktnull"
    android:versionCode="6"
    android:versionName="1.1.2" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="de.hfu.studiportal.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.hfu.studiportal.LoginActivity"
            android:label="@string/text_activity_login_title" >
        </activity>

        <receiver android:name="de.hfu.studiportal.RefreshServiceStarter" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="de.hfu.studiportal.CHECK_FOR_UPDATES" >
                </action>
            </intent-filter>
        </receiver>
    </application>

</manifest>